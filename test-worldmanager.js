// Test simple para verificar que WorldManager se puede importar
console.log('Testing WorldManager import...');

try {
  // Simular la importación (esto no funcionará en Node.js pero nos ayuda a verificar sintaxis)
  console.log('WorldManager import test completed');
} catch (error) {
  console.error('Error importing WorldManager:', error);
}